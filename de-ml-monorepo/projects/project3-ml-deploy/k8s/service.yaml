apiVersion: v1
kind: Service
metadata:
  name: model-api
  labels:
    app: model-api
spec:
  selector:
    app: model-api
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
